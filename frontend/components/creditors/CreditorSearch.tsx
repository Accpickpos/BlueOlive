'use client';

/**
 * Deprecated alias — CreditorPicker.tsx is the shared creditor/supplier
 * lookup component now (same SearchCombobox everything else uses). Kept as
 * a re-export so any existing import of CreditorSearch keeps working.
 */
export { CreditorPicker as CreditorSearch, default } from './CreditorPicker';
