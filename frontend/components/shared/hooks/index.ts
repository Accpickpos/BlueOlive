// Shared hooks exports
// Re-export hooks from ui/ for convenient access
export { usePagination } from '@/components/ui/pagination';

// Add custom shared hooks here as they are created
// Example:
// export { useDebounce } from './useDebounce';
// export { useLocalStorage } from './useLocalStorage';
